package com.cybertek.pages;


import org.testng.Assert;

public class DashboardPage extends BasePage{
    //no need to explicitly write constructor, because it will use its parentesis constructor.

}
